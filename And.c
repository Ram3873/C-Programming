#include <stdio.h>
int main()
{
    int age = 18, money = 0;
    if (age >= 18 && money == 1)
    {

        printf("can i buy a car\n");
    }
    if (age >= 19 || money)
    {
        printf("can we smoking legally\n");
    }

    return 0;
}

